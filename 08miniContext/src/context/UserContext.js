import React from "react";

      //variable           //method
const UserContext = React.createContext(); //component: will get the access of the global state

//throwing in the output
export default UserContext;